s = input()
#s를 정렬함.
ssort = ''.join(sorted(s))
#모든수가 0일수도 있어서 정렬을한뒤에 마지막 인덱스값을 m_num으로 저장함.
m_num = int(a[-1])
for i in range(len(ssort)-1):           #마지막 인덱스값을 저장했기 때문에 for범위는 0부터 ssort-1까지
    if int(ssort[i]) >= 2:                     #만약 숫자가 2이상이면 곱하고
        m_num *=int(ssort[i])
    else:
        m_num +=int(ssort[i])          #아니면 더함.
print(m_num) 
    





문자열 정렬    
s = "adfe"

s1 = s.sort()			# wrong
s2 = sorted(s)			# ['a','d','e','f'] 
s3 = ''.join(sorted(s))		# "adef"
